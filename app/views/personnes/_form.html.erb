<%= form_for(@personne) do |f| %>
  <% if @personne.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@personne.errors.count, "error") %> prohibited this personne from being saved:</h2>

      <ul>
      <% @personne.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nom %><br>
    <%= f.text_field :nom %>
  </div>
<div class="field">
 <% Scene.all.each do |scene| %>
 <label><%= scene.periode %>
   <%= check_box_tag "personne[scene_ids][]",
   scene.id,  @personne.scenes.include?(scene)%> 
 </label>
 <% end %>
</div>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
